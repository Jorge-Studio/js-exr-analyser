PyQt5>=5.15.0
numpy>=1.20.0
matplotlib>=3.5.0
Pillow>=9.0.0
pyqtgraph>=0.13.0
opencv-python-headless>=4.5.0
# OpenEXR is optional: use on macOS/Linux for full metadata. On Windows it often
# fails to build (needs Visual Studio). The app falls back to OpenCV for EXR reading.
OpenEXR>=3.0.0
